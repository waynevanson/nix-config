{
	"$schema": "https://opencode.ai/config.json",
	"instructions": ["instructions/*"],
	"plugin": ["./plugins/worktree/index.ts"],
	"command": {
		"worktree": {
			"description": "Implement a saved plan in a git worktree",
			"template": "You are running the worktree implementation workflow. Read the plan path from the user's command, then follow the instructions in the worktree skill to implement the plan in the prepared worktree, run verification, ask for acceptance, commit, remove the plan, merge back, and clean up."
		}
	},
	"permission": {
		"read": {
			"~/.cargo/registry/cache": "allow",
			"/nix/store/**": "allow",
			"**/target/**": "allow"
		}
	}
}
